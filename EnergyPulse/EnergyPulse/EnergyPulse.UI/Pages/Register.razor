@page "/register"
@using EnergyPulse.UI.Services
@inject AuthService AuthService
@inject NavigationManager Navigation

<div style="
    display:flex;
    justify-content:center;
    align-items:center;
    min-height:80vh;
    padding:20px;
    ">
    <div style="
        background:#161b22;
        padding:40px;
        border-radius:12px;
        border:1px solid #30363d;
        max-width:400px;
        width:100%;
        ">
        <h2 style="color:#2ecc71;text-align:center;margin-bottom:24px;">📝 Register</h2>

        @if (!string.IsNullOrEmpty(errorMessage))
        {
            <div style="
                background:#2d0f0f;
                border:1px solid #da3633;
                padding:10px;
                border-radius:6px;
                color:#da3633;
                margin-bottom:16px;
                font-size:14px;
                ">
                @errorMessage
            </div>
        }

        <div style="margin-bottom:16px;">
            <label style="color:#8b949e;font-size:13px;display:block;margin-bottom:6px;">Username</label>
            <input @bind="username" style="
            width:100%;
            padding:10px;
            background:#0d1117;
            border:1px solid #30363d;
            border-radius:6px;
            color:#e6edf3;
            " />
        </div>

        <div style="margin-bottom:16px;">
            <label style="color:#8b949e;font-size:13px;display:block;margin-bottom:6px;">Email</label>
            <input @bind="email" type="email" style="
            width:100%;
            padding:10px;
            background:#0d1117;
            border:1px solid #30363d;
            border-radius:6px;
            color:#e6edf3;
            " />
        </div>

        <div style="margin-bottom:20px;">
            <label style="color:#8b949e;font-size:13px;display:block;margin-bottom:6px;">Password</label>
            <input @bind="password" type="password" style="
            width:100%;
            padding:10px;
            background:#0d1117;
            border:1px solid #30363d;
            border-radius:6px;
            color:#e6edf3;
            " />
        </div>

        <button @onclick="HandleRegister" style="
        width:100%;
        padding:12px;
        background:#238636;
        color:white;
        border:none;
        border-radius:6px;
        font-size:16px;
        font-weight:600;
        cursor:pointer;
        ">Register</button>

        <p style="text-align:center;color:#8b949e;margin-top:16px;font-size:13px;">
            Already have an account? <a href="/login" style="color:#58a6ff;text-decoration:none;">Login</a>
        </p>
    </div>
</div>

@code {
    private string username = "";
    private string email = "";
    private string password = "";
    private string errorMessage = "";

    private async Task HandleRegister()
    {
        errorMessage = "";
        var success = await AuthService.Register(username, email, password);
        if (success)
        {
            // 🔥 Force a full page reload to refresh auth state
            Navigation.NavigateTo("/", forceLoad: true);
        }
        else
        {
            errorMessage = "Registration failed. Username or email may already exist.";
        }
    }
}