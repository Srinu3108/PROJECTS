@model FarmManagement.Web.Models.ViewModels.FarmAnalyticsViewModel
@{
    ViewData["Title"] = "Farm Analytics";
}

<div class="page-header">
    <div class="page-header-left">
        <h1>
            <span class="ph-icon" style="background:#e0f2fe;color:#0284c7;"><i class="bi bi-pie-chart"></i></span>
            Farm Analytics
        </h1>
        <p>Combined farm-wide performance metrics and breakdowns.</p>
    </div>
    <a asp-action="Index" class="btn-fm btn-outline-fm"><i class="bi bi-arrow-left"></i> Back to Reports</a>
</div>

<div class="row g-3 mb-4">
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#dcfce7;color:#16a34a;"><i class="bi bi-grid-3x3-gap"></i></div>
            <div><div class="stat-value">@Model.TotalFields</div><div class="stat-label">Fields (@Model.TotalFieldArea.ToString("N1") ha)</div></div>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#dbeafe;color:#2563eb;"><i class="bi bi-flower1"></i></div>
            <div><div class="stat-value">@Model.TotalCrops</div><div class="stat-label">Crops</div></div>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#ffedd5;color:#ea580c;"><i class="bi bi-box-seam"></i></div>
            <div><div class="stat-value">@Model.TotalResources</div><div class="stat-label">Resources (@Model.LowStockItems low)</div></div>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#dcfce7;color:#16a34a;"><i class="bi bi-graph-up"></i></div>
            <div><div class="stat-value">@Model.TotalYieldKg.ToString("N0") kg</div><div class="stat-label">Total Yield (@Model.TotalHarvests harvests)</div></div>
        </div>
    </div>
</div>

<div class="row g-3 mb-4">
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#fee2e2;color:#dc2626;"><i class="bi bi-bug"></i></div>
            <div><div class="stat-value" style="color:#dc2626;">@Model.ActivePests</div><div class="stat-label">Active Pests (of @Model.TotalPestIncidents)</div></div>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#dbeafe;color:#2563eb;"><i class="bi bi-calendar3"></i></div>
            <div><div class="stat-value">@Model.TotalSchedules</div><div class="stat-label">Schedules</div></div>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#fef9c3;color:#ca8a04;"><i class="bi bi-clock"></i></div>
            <div><div class="stat-value" style="color:#ca8a04;">@Model.ScheduledCount</div><div class="stat-label">Scheduled</div></div>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="stat-card">
            <div class="stat-icon" style="background:#dcfce7;color:#16a34a;"><i class="bi bi-check-circle"></i></div>
            <div><div class="stat-value" style="color:#16a34a;">@Model.CompletedCount</div><div class="stat-label">Completed</div></div>
        </div>
    </div>
</div>

<div class="row g-3">
    <div class="col-md-6">
        <div class="fm-card">
            <div class="fm-card-header">
                <span class="ch-title"><i class="bi bi-flower1" style="color:#2563eb;"></i> Crops by Status</span>
            </div>
            <div class="fm-card-body">
                @if (Model.CropsByStatus.Any())
                {
                    <table class="fm-table">
                        <thead><tr><th>Status</th><th>Count</th></tr></thead>
                        <tbody>
                            @foreach (var s in Model.CropsByStatus)
                            {
                                <tr><td><span class="badge-pill badge-gray">@s.Status</span></td><td style="font-weight:700;">@s.Count</td></tr>
                            }
                        </tbody>
                    </table>
                }
                else { <p class="text-muted small">No crops found.</p> }
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="fm-card">
            <div class="fm-card-header">
                <span class="ch-title"><i class="bi bi-box-seam" style="color:#ea580c;"></i> Resources by Type</span>
            </div>
            <div class="fm-card-body">
                @if (Model.ResourcesByType.Any())
                {
                    <table class="fm-table">
                        <thead><tr><th>Type</th><th>Count</th><th>Total Qty</th></tr></thead>
                        <tbody>
                            @foreach (var t in Model.ResourcesByType)
                            {
                                <tr>
                                    <td><span class="badge-pill badge-gray">@t.Type</span></td>
                                    <td style="font-weight:700;">@t.Count</td>
                                    <td>@t.TotalQty.ToString("N0")</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else { <p class="text-muted small">No resources found.</p> }
            </div>
        </div>
    </div>
</div>

<!-- Pest Incidents by Crop -->
<div class="fm-card mt-4">
    <div class="fm-card-header">
        <span class="ch-title"><i class="bi bi-bug" style="color:#dc2626;"></i> Pest Incidents by Crop</span>
    </div>
    <div class="fm-card-body">
        @if (Model.PestsByCrop.Any())
        {
            <table class="fm-table">
                <thead><tr><th>Crop</th><th>Active</th><th>Monitoring</th><th>Resolved</th><th>Total</th></tr></thead>
                <tbody>
                    @foreach (var c in Model.PestsByCrop)
                    {
                        <tr>
                            <td style="font-weight:600;">@c.CropName</td>
                            <td><span class="badge-pill badge-red">@c.Active</span></td>
                            <td><span class="badge-pill badge-yellow">@c.Monitoring</span></td>
                            <td><span class="badge-pill badge-green">@c.Resolved</span></td>
                            <td style="font-weight:700;">@c.Total</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else { <p class="text-muted small">No pest incidents found.</p> }
    </div>
</div>
